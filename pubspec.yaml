name: medeye_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.9.2 <4.0.0"  # Thêm range version cho SDK

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  smooth_page_indicator: ^1.1.0

  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  
  image_picker: ^1.0.4 
  onnxruntime: ^1.4.1    
  opencv_dart: ^1.1.2         
  path_provider: ^2.1.1  
  path: ^1.8.3
  google_mlkit_text_recognition: ^0.13.0
  google_generative_ai: ^0.4.0
  intl: ^0.19.0
  ffi: ^2.1.2  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "lib/assets/images/Frog-MedEye.png" 
  min_sdk_android: 21

flutter:
  uses-material-design: true
  
  assets:
    - lib/assets/images/
    - lib/assets/models/